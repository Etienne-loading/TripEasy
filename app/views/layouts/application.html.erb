<!DOCTYPE html>
<html>
  <head>
    <title>TripEasy</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
    <%= favicon_link_tag "logo-tripeasy-square.png", rel: "icon", type: "image/png" %>
  </head>

  <body>
    <% if action_name != "home" %>
      <%= render "shared/navbar" %>
    <% end %>
    <%= render "shared/flashes" %>

    <div style="min-height: calc(100vh - 274px); position: relative;">
      <%= yield %>
    </div>

    <%= render "shared/footer" if action_name == "home" || (action_name == "index" && controller_name == "blogs") %>
  </body>
</html>
