<%= render "shared/banner" %>
<div class="py-5 bg-white">
  <div class="container">
    <div class="row">
        <h2>Find popular destinations</h2>
      </div>
      <div class="row">
        <div class="col">
          <div class="card-container">
            <% counter = 0 %>
            <% @blogs.each do |blog| %>
            <% if counter < 4 %>
            <%= link_to blog_path(blog), class: "card-link" do %>
            <div class="card-img" style="background-image: url(<%= blog.photo_banner.attached? ? cl_image_path(blog.photo_banner.key) : image_path("default-bg.jpg") %>)"></div>
              <div class="card-blog p-3">
                <div class="d-flex flex-column">
                    <div class="mt-2"><span class="card-title"><%= blog.title %></span></div>
                      <div>
                        <% if (blog.end_date - blog.start_date).to_i < 14 %>
                        <span class="duration-price">€<%= (blog.max_price + blog.min_price) / 2 %> | <%= (blog.end_date - blog.start_date).to_i %> days </span>
                        <% elsif (blog.end_date - blog.start_date).to_i >= 14 && (blog.end_date - blog.start_date).to_i < 60 %>
                        <span class="duration-price">€<%= ((blog.max_price + blog.min_price) / 2) * 4 %> | <%= ((blog.end_date - blog.start_date).to_i) / 7 %> weeks </span>
                        <% elsif (blog.end_date - blog.start_date).to_i >= 60 %>
                        <span class="duration-price">€<%= (blog.max_price + blog.min_price) / 2 %> | <%= ((blog.end_date - blog.start_date).to_i) / 30 %> months </span>
                        <% end %>
                        <div class="mt-2">
                          <% used_tags = [] %>
                      <% blog.steps.sample(3).each do |step| %>
                        <% if step.tag_list.present? %>
                        <% available_tags = step.tag_list - used_tags %>
                        <% selected_tags = available_tags.sample(3) %>
                        <% used_tags += selected_tags %>
                        <% selected_tags.sample(3).each do |tag| %>
                          <span class="tag mt-1">#<%= tag %></span> &nbsp;
                        <% end %>
                      <% end %>
                    <% end %>
                        </div>
                        <div style="display: flex; justify-content: space-between; margin-top: 25px;">
                          <div class="localisation">
                            <%= image_tag "localisation.svg",style:"max-height: 24px; max-width: 24px;", class:"me-2" %>
                            <% if blog.steps.first %>
                              <%=blog.steps.first.country%>
                            <% end %>
                          </div>
                          <div class="heart"> <%= rand(1..99) %> <%= image_tag "heart.svg", style:"max-height: 24px; max-width: 24px;", class:"ms-2" %></div>
                        </div>
                      </div>
                    </div>
                </div>
                <% end %>
                <% end %>
                <% counter += 1 %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="py-5 bg-white">
  <div class="container">
    <div class="row">
        <h2>Find popular countries</h2>
      </div>
      <div class="row">
        <div class="col">
          <div class="card-container">
            <% counter = 0 %>
            <% @blogs.each do |blog| %>
            <% if counter < 4 %>
            <%= link_to blog_path(blog), class: "card-link" do %>
            <div class="card-img" style="background-image: url(<%= blog.photo_banner.attached? ? cl_image_path(blog.photo_banner.key) : image_path("default-bg.jpg") %>)"></div>
              <div class="card-blog p-3">
                <div class="d-flex flex-column">
                    <div class="mt-2"><span class="card-title"><%= blog.title %></span></div>
                      <div>
                        <% if (blog.end_date - blog.start_date).to_i < 14 %>
                        <span class="duration-price">€<%= (blog.max_price + blog.min_price) / 2 %> | <%= (blog.end_date - blog.start_date).to_i %> days </span>
                        <% elsif (blog.end_date - blog.start_date).to_i >= 14 && (blog.end_date - blog.start_date).to_i < 60 %>
                        <span class="duration-price">€<%= ((blog.max_price + blog.min_price) / 2) * 4 %> | <%= ((blog.end_date - blog.start_date).to_i) / 7 %> weeks </span>
                        <% elsif (blog.end_date - blog.start_date).to_i >= 60 %>
                        <span class="duration-price">€<%= (blog.max_price + blog.min_price) / 2 %> | <%= ((blog.end_date - blog.start_date).to_i) / 30 %> months </span>
                        <% end %>
                        <div class="mt-2">
                          <% used_tags = [] %>
                      <% blog.steps.sample(3).each do |step| %>
                        <% if step.tag_list.present? %>
                        <% available_tags = step.tag_list - used_tags %>
                        <% selected_tags = available_tags.sample(3) %>
                        <% used_tags += selected_tags %>
                        <% selected_tags.sample(3).each do |tag| %>
                          <span class="tag mt-1">#<%= tag %></span> &nbsp;
                        <% end %>
                      <% end %>
                    <% end %>
                        </div>
                        <div style="display: flex; justify-content: space-between; margin-top: 25px;">
                          <div class="localisation">
                            <%= image_tag "localisation.svg",style:"max-height: 24px; max-width: 24px;", class:"me-2" %>
                            <% if blog.steps.first %>
                              <%=blog.steps.first.country%>
                            <% end %>
                          </div>
                          <div class="heart"> <%= rand(1..99) %> <%= image_tag "heart.svg", style:"max-height: 24px; max-width: 24px;", class:"ms-2" %></div>
                        </div>
                      </div>
                    </div>
                </div>
                <% end %>
                <% end %>
                <% counter += 1 %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="py-5 bg-white">
  <div class="container">
    <div class="row">
        <h2>Find popular categories</h2>
      </div>
      <div class="row">
        <div class="col">
          <div class="card-container">
            <% counter = 0 %>
            <% @blogs.each do |blog| %>
            <% if counter < 4 %>
            <%= link_to blog_path(blog), class: "card-link" do %>
            <div class="card-img" style="background-image: url(<%= blog.photo_banner.attached? ? cl_image_path(blog.photo_banner.key) : image_path("default-bg.jpg") %>)"></div>
              <div class="card-blog p-3">
                <div class="d-flex flex-column">
                    <div class="mt-2"><span class="card-title"><%= blog.title %></span></div>
                      <div>
                        <% if (blog.end_date - blog.start_date).to_i < 14 %>
                        <span class="duration-price">€<%= (blog.max_price + blog.min_price) / 2 %> | <%= (blog.end_date - blog.start_date).to_i %> days </span>
                        <% elsif (blog.end_date - blog.start_date).to_i >= 14 && (blog.end_date - blog.start_date).to_i < 60 %>
                        <span class="duration-price">€<%= ((blog.max_price + blog.min_price) / 2) * 4 %> | <%= ((blog.end_date - blog.start_date).to_i) / 7 %> weeks </span>
                        <% elsif (blog.end_date - blog.start_date).to_i >= 60 %>
                        <span class="duration-price">€<%= (blog.max_price + blog.min_price) / 2 %> | <%= ((blog.end_date - blog.start_date).to_i) / 30 %> months </span>
                        <% end %>
                        <div class="mt-2">
                          <span class="tag mt-1">#surfing </span>
                          <span class="tag mt-1">#hiking </span>
                          <span class="tag">#hunting </span>
                        </div>
                        <div style="display: flex; justify-content: space-between; margin-top: 25px;">
                          <div class="localisation">
                            <%= image_tag "localisation.svg",style:"max-height: 24px; max-width: 24px;", class:"me-2" %>
                            <% if blog.steps.first %>
                              <%=blog.steps.first.country%>
                            <% end %>
                          </div>
                          <div class="heart"> <%= rand(1..99) %> <%= image_tag "heart.svg", style:"max-height: 24px; max-width: 24px;", class:"ms-2" %></div>
                        </div>
                      </div>
                    </div>
                </div>
                <% end %>
                <% end %>
                <% counter += 1 %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
