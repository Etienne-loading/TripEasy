<div class="banner">
  <div class="container">
    <%= render "shared/navbar" %>
    <div class="banner-grid">
      <div class="banner-content">
        <h1>Explore <br> The <strong>World</strong></h1>
        <p><strong>TripEasy</strong> : The ultimate travel blog for adventurers looking for inspiration, advice and compelling stories.</p>
        <%= form_with url: blogs_path, method: :get, class: "d-flex" do %>
          <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Nevada, Road-trip, hiking, etc..."
          %>
          <%= submit_tag "Search", class: "btn btn-primary" %>
        <% end %>
        <div class="arrow"></div>
      </div>
    </div>
  </div>
</div>
